import Gallery from '../components/Gallery';

export default function Events() {
  return (
    <Gallery 
      category="events" 
      title="Events" 
      description="From corporate gatherings to grand celebrations, we capture the energy and essence of every event." 
    />
  );
}
